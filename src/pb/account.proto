syntax = 'proto3';

message SendParam {
  bytes toAddress = 1;
}

message filters {
    
}

message accountResult {
}



service Account {

rpc GetAccounts (filters) returns (accountResult);

rpc GetAccount (SendParam) returns (accountResult);

rpc GetSequence (SendParam) returns (accountSequence);

rpc GeStorage (SendParam) returns ();

rpc GetStorageAt (SendParam) return ();
}